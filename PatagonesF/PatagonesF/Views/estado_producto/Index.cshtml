@model IEnumerable<PatagonesF.estado_producto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo_estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_estado_producto }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_estado_producto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_estado_producto })
        </td>
    </tr>
}

</table>
