@model IEnumerable<PatagonesF.telefono_personal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.num1_personal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.num2_personal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.personal.nombre_completo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.num1_personal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.num2_personal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.personal.nombre_completo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_telefono_personal }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_telefono_personal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_telefono_personal })
        </td>
    </tr>
}

</table>
