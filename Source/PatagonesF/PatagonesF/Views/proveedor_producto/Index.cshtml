@model IEnumerable<PatagonesF.proveedor_producto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.producto.producto1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.proveedor.nombre_proveedor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.producto.producto1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.proveedor.nombre_proveedor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_producto_proveedor }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_producto_proveedor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_producto_proveedor })
        </td>
    </tr>
}

</table>
